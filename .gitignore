# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
.venv/
env/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Data directories (can be large)
data/
contracts/
*.db
*.sqlite

# Environment variables
.env
.env.local
.env.*.local

# Docker
.dockerignore

# External dependencies
externals/

# Temporary files
*.tmp
*.temp

# PyPI publishing
.pypirc
*.pypirc
.pypirc.*
kv-store-benchmark-report.txt

# Workflow example bundles (built from calimero-network/core by workflow-examples/scripts/build_res_bundles.sh)
workflow-examples/res/*.mpk
example-project/res/*.mpk
# Nothing in these trees is a source file. The bundle script used to emit raw
# .wasm here too, and an ignore covering only *.mpk is how a 474 KB
# kv_store.wasm reached master in #404 and had to be reverted in #405 — and how
# a stale local copy nearly rode along again. Ignore the outputs, not one
# extension of them.
workflow-examples/res/*.wasm
example-project/res/*.wasm
# Staged bundle tree served by workflow-examples/scripts/fixture-registry.sh
fixture-registry/
workflow-examples/.core-repo/
.claude/
